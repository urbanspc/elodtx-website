# EloDtx Website Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. Read the full spec at `docs/superpowers/specs/2026-03-30-elodtx-website-design.md` for complete copy and content details.

**Goal:** Build a complete, production-ready EloDtx marketing website with 8 pages, 25+ components, dark theme, gold accents, and world-class animations.

**Architecture:** Next.js 14+ App Router with server components for pages and client components for interactive sections. All content data centralized in `lib/constants.ts`. Framer Motion for animations, shadcn/ui for base components, Tailwind for styling.

**Tech Stack:** Next.js 14, TypeScript, Tailwind CSS, Framer Motion, shadcn/ui, Lucide React, Recharts, Shiki (syntax highlighting)

---

## File Map

### Configuration & Setup
- `package.json` — dependencies
- `next.config.ts` — Next.js config
- `tailwind.config.ts` — custom colors, fonts, spacing
- `tsconfig.json` — TypeScript config (generated by Next.js)
- `components.json` — shadcn/ui config
- `styles/globals.css` — Tailwind directives, CSS custom properties
- `postcss.config.mjs` — PostCSS config

### Foundation
- `lib/utils.ts` — `cn()` helper (shadcn/ui pattern)
- `lib/constants.ts` — all site content, copy, data arrays
- `app/layout.tsx` — root layout with fonts, metadata, nav, footer
- `components/layout/navbar.tsx` — sticky nav with mobile menu
- `components/layout/footer.tsx` — 4-column footer

### Shared Components
- `components/shared/section-reveal.tsx` — scroll-triggered Framer Motion wrapper
- `components/shared/animated-counter.tsx` — count-up animation on scroll
- `components/shared/animated-connection-graph.tsx` — SVG node network background
- `components/shared/agent-card.tsx` — expandable agent description card
- `components/shared/compatibility-score.tsx` — circular progress indicator
- `components/shared/conversation-demo.tsx` — animated chat with whisper
- `components/shared/proximity-alert-mockup.tsx` — Apple Watch alert card
- `components/shared/waitlist-form.tsx` — multi-mode form with validation
- `components/shared/pricing-calculator.tsx` — MAU slider with cost display
- `components/shared/code-block.tsx` — syntax-highlighted code with copy + language tabs

### Home Page Components
- `components/home/hero.tsx` — animated hero with connection graph background
- `components/home/stats-strip.tsx` — 6 animated metric counters
- `components/home/how-it-works.tsx` — 5-step process section
- `components/home/agents-section.tsx` — 5 agent cards grid
- `components/home/industries-section.tsx` — 6-tab vertical showcase
- `components/home/comparison-table.tsx` — 12-row feature comparison
- `components/home/proof-section.tsx` — 3 proof metrics + testimonial
- `components/home/pricing-section.tsx` — 3-tier pricing cards
- `components/home/final-cta.tsx` — compact waitlist form CTA

### Page Components
- `components/how-it-works/whisper-system-diagram.tsx`
- `components/how-it-works/proximity-flow-steps.tsx`
- `components/how-it-works/behavior-learning-loop.tsx`
- `components/how-it-works/training-path-timeline.tsx`
- `components/the-whisper/conversation-comparison.tsx`
- `components/the-whisper/whisper-sources-diagram.tsx`
- `components/proximity/geohash-privacy-diagram.tsx`
- `components/proximity/consent-flow-diagram.tsx`
- `components/proximity/proximity-faq.tsx`
- `components/developers/endpoint-doc.tsx`
- `components/developers/language-tabs.tsx`
- `components/developers/rate-limits-table.tsx`
- `components/about/timeline.tsx`
- `components/about/team-structure.tsx`

### Pages
- `app/page.tsx` — Home
- `app/how-it-works/page.tsx`
- `app/industries/page.tsx`
- `app/the-whisper/page.tsx`
- `app/proximity/page.tsx`
- `app/developers/page.tsx`
- `app/about/page.tsx`
- `app/waitlist/page.tsx`
- `app/api/waitlist/route.ts` — form endpoint

---

## Task 1: Project Scaffolding & Configuration

**Files:**
- Create: `package.json`, `next.config.ts`, `tailwind.config.ts`, `postcss.config.mjs`, `styles/globals.css`, `components.json`, `lib/utils.ts`, `.gitignore`

- [ ] **Step 1: Create Next.js project**

```bash
cd /c/Users/steph
npx create-next-app@latest elodtx-website --typescript --tailwind --eslint --app --src=no --import-alias="@/*" --use-npm
```

Accept defaults. This generates the base project with `app/` directory, Tailwind, TypeScript.

- [ ] **Step 2: Install dependencies**

```bash
cd /c/Users/steph/elodtx-website
npm install framer-motion lucide-react recharts class-variance-authority clsx tailwind-merge shiki
npm install -D @types/node
```

- [ ] **Step 3: Initialize shadcn/ui**

```bash
cd /c/Users/steph/elodtx-website
npx shadcn@latest init
```

When prompted, select:
- Style: Default
- Base color: Slate
- CSS variables: Yes

- [ ] **Step 4: Install shadcn/ui components**

```bash
cd /c/Users/steph/elodtx-website
npx shadcn@latest add button card tabs input textarea select badge separator
```

- [ ] **Step 5: Configure Tailwind theme**

Replace `tailwind.config.ts` with:

```typescript
import type { Config } from "tailwindcss";

const config: Config = {
  darkMode: ["class"],
  content: [
    "./pages/**/*.{ts,tsx}",
    "./components/**/*.{ts,tsx}",
    "./app/**/*.{ts,tsx}",
    "./lib/**/*.{ts,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        brand: {
          gold: "#C9A84C",
          "gold-light": "#D4B85C",
          "gold-dark": "#B8963C",
        },
        dark: {
          primary: "#0D0D1A",
          secondary: "#1A1A2E",
          tertiary: "#252540",
        },
        success: "#1D9E75",
        error: "#E54D4D",
      },
      fontFamily: {
        sans: ["var(--font-inter)", "system-ui", "sans-serif"],
        mono: ["var(--font-jetbrains)", "monospace"],
      },
      maxWidth: {
        site: "1280px",
      },
    },
  },
  plugins: [require("tailwindcss-animate")],
};

export default config;
```

- [ ] **Step 6: Configure globals.css**

Replace `styles/globals.css` (or `app/globals.css` depending on where create-next-app put it) with:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --brand-gold: #C9A84C;
    --brand-gold-light: #D4B85C;
    --brand-gold-dark: #B8963C;
    --dark-primary: #0D0D1A;
    --dark-secondary: #1A1A2E;
    --dark-tertiary: #252540;
    --text-primary: rgba(255, 255, 255, 0.85);
    --text-secondary: rgba(255, 255, 255, 0.6);
    --text-muted: rgba(255, 255, 255, 0.4);
  }

  * {
    border-color: var(--dark-tertiary);
  }

  body {
    background-color: var(--dark-primary);
    color: var(--text-primary);
    font-feature-settings: "rlig" 1, "calt" 1;
  }

  ::selection {
    background-color: rgba(201, 168, 76, 0.3);
    color: white;
  }
}

@layer utilities {
  .gold-gradient {
    background: linear-gradient(135deg, #C9A84C, #D4B85C);
  }

  .gold-glow {
    box-shadow: 0 0 40px rgba(201, 168, 76, 0.15);
  }

  .gold-glow-strong {
    box-shadow: 0 0 60px rgba(201, 168, 76, 0.25);
  }

  .text-gradient-gold {
    background: linear-gradient(135deg, #C9A84C, #D4B85C);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
}

/* Scrollbar styling */
::-webkit-scrollbar {
  width: 8px;
  height: 8px;
}

::-webkit-scrollbar-track {
  background: var(--dark-primary);
}

::-webkit-scrollbar-thumb {
  background: var(--dark-tertiary);
  border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
  background: var(--brand-gold-dark);
}
```

- [ ] **Step 7: Configure next.config.ts**

```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
```

- [ ] **Step 8: Ensure lib/utils.ts has cn helper**

shadcn/ui init should create this, but verify it contains:

```typescript
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

- [ ] **Step 9: Verify build**

```bash
cd /c/Users/steph/elodtx-website
npm run build
```

Expected: Build succeeds with no errors.

- [ ] **Step 10: Commit**

```bash
cd /c/Users/steph/elodtx-website
git add -A
git commit -m "feat: scaffold Next.js project with Tailwind, shadcn/ui, and brand theme"
```

---

## Task 2: Constants — All Site Content Data

**Files:**
- Create: `lib/constants.ts`

This is the single source of truth for all copy, data arrays, pricing, API examples, and content used across the site. Every component reads from here.

- [ ] **Step 1: Create lib/constants.ts with all type definitions and data**

```typescript
import {
  Brain,
  MessageCircle,
  Clock,
  AlertTriangle,
  Shield,
  Plug,
  Users,
  BarChart3,
  Zap,
  Heart,
  Briefcase,
  Stethoscope,
  Handshake,
  Building2,
  GraduationCap,
  type LucideIcon,
} from "lucide-react";

// ─── Stats ───────────────────────────────────────────────────────────────────

export interface Stat {
  value: string;
  numericValue: number;
  suffix: string;
  prefix: string;
  label: string;
}

export const STATS: Stat[] = [
  { value: "138", numericValue: 138, suffix: "", prefix: "", label: "Preference dimensions analyzed" },
  { value: "4.9", numericValue: 4.9, suffix: "x", prefix: "", label: "Lighter than standard AI models" },
  { value: "15.11", numericValue: 15.11, suffix: "%", prefix: "", label: "Real-world meeting conversion rate" },
  { value: "5", numericValue: 5, suffix: "", prefix: "", label: "Specialized AI agents" },
  { value: "2", numericValue: 2, suffix: "s", prefix: "<", label: "Real-time proximity scoring" },
  { value: "3", numericValue: 3, suffix: "", prefix: "", label: "Languages (growing)" },
];

// ─── How It Works ────────────────────────────────────────────────────────────

export interface Step {
  icon: LucideIcon;
  title: string;
  description: string;
}

export const HOW_IT_WORKS_STEPS: Step[] = [
  {
    icon: Plug,
    title: "You integrate the API",
    description: "One integration connects EloDtx to your platform. RESTful API, JWT auth, SDKs in JavaScript, Python, and Swift.",
  },
  {
    icon: Brain,
    title: "EloDtx learns your users",
    description: "138-dimension behavioral profiling from day one. The Know Me engine builds granular profiles that get smarter with every interaction.",
  },
  {
    icon: Users,
    title: "It connects the right people",
    description: "Compatibility scores, conversation openers, match timing — all computed in real time across your entire user base.",
  },
  {
    icon: MessageCircle,
    title: "It keeps them connected",
    description: "Churn prediction, conversation health monitoring, and automated re-engagement keep relationships alive.",
  },
  {
    icon: BarChart3,
    title: "You see everything",
    description: "Full analytics dashboard with actionable insights. Retention curves, engagement heatmaps, conversion funnels.",
  },
];

// ─── Agents ──────────────────────────────────────────────────────────────────

export interface Agent {
  icon: LucideIcon;
  name: string;
  subtitle: string;
  shortDescription: string;
  fullDescription: string;
}

export const AGENTS: Agent[] = [
  {
    icon: Brain,
    name: "Know Me",
    subtitle: "Preference Engine",
    shortDescription: "138 paired-choice questions across 10 psychological dimensions.",
    fullDescription:
      "138 paired-choice questions across 10 psychological dimensions. Builds granular user profiles that get smarter with every interaction. Passive behavioral signals — scroll patterns, session timing, notification response — continuously refine the model without requiring additional user input.",
  },
  {
    icon: MessageCircle,
    name: "Conversation Health",
    subtitle: "Agent",
    shortDescription: "Monitors every exchange in real time for tone shifts and stagnation.",
    fullDescription:
      "Monitors every exchange in real time. Detects tone shifts, stagnation and disengagement. Triggers intelligent interventions before a conversation dies. Processes signals ephemerally — message content is never stored, only behavioral patterns are extracted.",
  },
  {
    icon: Clock,
    name: "Timing Optimization",
    subtitle: "Agent",
    shortDescription: "Learns the exact moment each user is most receptive.",
    fullDescription:
      "Learns the exact moment each individual user is most receptive. Schedules all communications at optimal times across all channels. Adapts to timezone changes, habit shifts, and seasonal patterns automatically.",
  },
  {
    icon: AlertTriangle,
    name: "Churn Detection",
    subtitle: "Agent",
    shortDescription: "Predicts disengagement before it happens.",
    fullDescription:
      "Predicts disengagement before it happens. Identifies users at risk and automatically triggers personalized retention interventions. Detects early warning signals — decreased session frequency, shorter interactions, ignored notifications — up to 14 days before churn.",
  },
  {
    icon: Shield,
    name: "Behavioral Watchdog",
    subtitle: "",
    shortDescription: "Monitors for abuse, spam and manipulation in real time.",
    fullDescription:
      "Monitors for abuse, spam and manipulation. Adjusts user trust scores dynamically. Keeps your platform safe and premium automatically. Detects fake profiles, coordinated inauthentic behavior, and harassment patterns without false-flagging genuine users.",
  },
];

// ─── Industries ──────────────────────────────────────────────────────────────

export interface IndustryFeature {
  title: string;
  description: string;
}

export interface Industry {
  icon: LucideIcon;
  id: string;
  name: string;
  tagline: string;
  description: string;
  problem: string;
  problemStats: string;
  features: IndustryFeature[];
  activeAgents: string[];
  outcomes: string[];
  caseStudy?: string;
}

export const INDUSTRIES: Industry[] = [
  {
    icon: Heart,
    id: "dating",
    name: "Dating & Social",
    tagline: "Stop matching profiles. Start connecting people.",
    description:
      "EloDtx replaces shallow swipe-based matching with deep compatibility intelligence that predicts real-world chemistry.",
    problem: "Dating apps have a retention crisis. Users churn within 90 days because matches don't lead to meaningful connections.",
    problemStats: "The average dating app user spends 90 minutes per day swiping but converts less than 2% of matches into real dates.",
    features: [
      { title: "Deep compatibility scoring", description: "138-dimension profiles that go beyond photos and bios to predict real-world chemistry." },
      { title: "Fake profile elimination", description: "Behavioral Watchdog detects inauthenticity through interaction patterns, not just photo verification." },
      { title: "Conversation rescue", description: "Conversation Health Agent detects dying exchanges and surfaces contextual whispers to deepen dialogue." },
      { title: "Safe Proximity IRL bridge", description: "Turn digital matches into real-world encounters with privacy-preserving geolocation and Apple Watch alerts." },
    ],
    activeAgents: ["Know Me", "Conversation Health", "Timing Optimization", "Churn Detection", "Behavioral Watchdog"],
    outcomes: [
      "15.11% digital-to-IRL conversion rate (vs industry average of ~2%)",
      "3x longer average conversation length",
      "60% reduction in reported fake profiles",
    ],
    caseStudy: "Baeyond POC in Mauritius & La Réunion: 508 real users, 15.11% digital-to-IRL conversion, 100% sold-out Âme Intense events.",
  },
  {
    icon: Briefcase,
    id: "hr",
    name: "HR & Recruitment",
    tagline: "Culture fit is human. Now AI can measure it.",
    description:
      "EloDtx maps team dynamics and predicts which candidates will thrive — not just perform — in your organization.",
    problem: "Bad hires cost 30% of the employee's annual salary. Most hiring tools measure skills but ignore the human dynamics that determine retention.",
    problemStats: "46% of new hires fail within 18 months, and 89% of the time it's due to cultural misfit, not skill gaps.",
    features: [
      { title: "Team compatibility mapping", description: "Understand how a new hire will interact with existing team members before they start." },
      { title: "Retention risk scoring", description: "Churn Detection identifies employees showing early disengagement signals." },
      { title: "Onboarding connection engine", description: "Automatically connect new hires with compatible mentors and collaborators." },
    ],
    activeAgents: ["Know Me", "Churn Detection", "Timing Optimization"],
    outcomes: [
      "40% reduction in early-stage turnover",
      "2x faster cultural integration for new hires",
      "Measurable team cohesion improvement within 90 days",
    ],
  },
  {
    icon: Stethoscope,
    id: "wellness",
    name: "Wellness & Therapy",
    tagline: "The right support, at the right moment.",
    description:
      "EloDtx matches individuals with the practitioners and communities most likely to help them, based on deep behavioral understanding.",
    problem: "Finding the right therapist is trial and error. 50% of therapy relationships end prematurely due to poor fit.",
    problemStats: "The average person tries 3 therapists before finding one that works. Each failed attempt delays recovery by months.",
    features: [
      { title: "Practitioner matching", description: "Match patients with therapists based on communication style, values, and therapeutic approach compatibility." },
      { title: "Emotional state tracking", description: "Detect shifts in user engagement patterns that may indicate changing emotional states." },
      { title: "Community connection", description: "Connect users with peer support groups where they're most likely to engage and benefit." },
    ],
    activeAgents: ["Know Me", "Conversation Health", "Timing Optimization", "Churn Detection"],
    outcomes: [
      "65% improvement in practitioner-patient match quality",
      "3x increase in long-term therapeutic engagement",
      "Reduced dropout rates through early intervention",
    ],
  },
  {
    icon: Handshake,
    id: "networking",
    name: "Professional Networking",
    tagline: "Most connections are hollow. EloDtx makes them real.",
    description:
      "EloDtx transforms networking from business card exchanges into genuinely valuable professional relationships.",
    problem: "Professional networking is broken. 80% of LinkedIn connections never exchange a single message after connecting.",
    problemStats: "Professionals spend an average of 6 hours per month networking but rate only 11% of new connections as 'valuable'.",
    features: [
      { title: "High-value introduction engine", description: "Identify and facilitate connections with the highest mutual value potential." },
      { title: "Event networking bridge", description: "Pre-match attendees before events and facilitate meaningful conversations during them." },
      { title: "Mentorship matching", description: "Connect mentors and mentees based on complementary skills, experience, and communication styles." },
    ],
    activeAgents: ["Know Me", "Timing Optimization", "Conversation Health"],
    outcomes: [
      "4x increase in post-connection engagement",
      "70% of facilitated introductions lead to follow-up meetings",
      "Measurable professional development acceleration",
    ],
  },
  {
    icon: Building2,
    id: "enterprise",
    name: "Enterprise & Teams",
    tagline: "Build teams that actually work together.",
    description:
      "EloDtx maps team dynamics in real time and identifies friction, isolation, and collaboration opportunities before they become problems.",
    problem: "Remote and hybrid teams struggle with cohesion. Managers can't see interpersonal dynamics through Slack messages and Zoom calls.",
    problemStats: "67% of remote workers report feeling disconnected from colleagues. Teams with poor cohesion are 50% less productive.",
    features: [
      { title: "Team dynamics mapping", description: "Real-time visualization of team interaction patterns, collaboration strength, and communication gaps." },
      { title: "Remote cohesion engine", description: "Identify isolated team members and facilitate organic connection opportunities." },
      { title: "Friction detection", description: "Detect interpersonal friction early through communication pattern analysis." },
    ],
    activeAgents: ["Know Me", "Conversation Health", "Churn Detection", "Behavioral Watchdog"],
    outcomes: [
      "35% improvement in cross-team collaboration metrics",
      "50% faster identification of team friction points",
      "Measurable increase in employee satisfaction scores",
    ],
  },
  {
    icon: GraduationCap,
    id: "education",
    name: "Education & Mentorship",
    tagline: "Learning happens through connection.",
    description:
      "EloDtx creates compatible study groups, predicts dropout risk, and matches mentors at scale.",
    problem: "Online education has a completion crisis. Most students learn alone and drop out before finishing.",
    problemStats: "MOOC completion rates average 5-15%. Students who form study groups are 5x more likely to complete a course.",
    features: [
      { title: "Compatible study groups", description: "Form study groups based on learning style, schedule, knowledge level, and personality compatibility." },
      { title: "Dropout prediction", description: "Identify at-risk students through engagement pattern analysis and trigger early intervention." },
      { title: "Mentor matching at scale", description: "Connect students with mentors whose experience and teaching style match their learning needs." },
    ],
    activeAgents: ["Know Me", "Churn Detection", "Timing Optimization"],
    outcomes: [
      "3x improvement in course completion rates",
      "80% of study groups remain active through course completion",
      "Early dropout intervention success rate above 60%",
    ],
  },
];

// ─── Comparison Table ────────────────────────────────────────────────────────

export interface ComparisonRow {
  capability: string;
  generalAI: "yes" | "no" | "partial";
  standardAlgo: "yes" | "no" | "partial";
  elodtx: "yes" | "no" | "partial";
}

export const COMPARISON_DATA: ComparisonRow[] = [
  { capability: "Purpose-built for connection", generalAI: "no", standardAlgo: "no", elodtx: "yes" },
  { capability: "Multi-agent architecture", generalAI: "no", standardAlgo: "no", elodtx: "yes" },
  { capability: "Real-time conversation health", generalAI: "partial", standardAlgo: "no", elodtx: "yes" },
  { capability: "Churn prediction", generalAI: "no", standardAlgo: "no", elodtx: "yes" },
  { capability: "138-dimension profiling", generalAI: "no", standardAlgo: "partial", elodtx: "yes" },
  { capability: "Behavioral fake detection", generalAI: "partial", standardAlgo: "no", elodtx: "yes" },
  { capability: "Timing optimization per user", generalAI: "no", standardAlgo: "no", elodtx: "yes" },
  { capability: "Real-world event bridge", generalAI: "no", standardAlgo: "no", elodtx: "yes" },
  { capability: "Multi-cultural by design", generalAI: "partial", standardAlgo: "no", elodtx: "yes" },
  { capability: "4.9x compressed — low cost", generalAI: "no", standardAlgo: "no", elodtx: "yes" },
  { capability: "Trained on real behavioral data", generalAI: "no", standardAlgo: "partial", elodtx: "yes" },
  { capability: "Fully owned IP", generalAI: "no", standardAlgo: "partial", elodtx: "yes" },
];

// ─── Proof Metrics ───────────────────────────────────────────────────────────

export interface ProofMetric {
  value: string;
  numericValue: number;
  suffix: string;
  label: string;
  detail: string;
}

export const PROOF_METRICS: ProofMetric[] = [
  {
    value: "508",
    numericValue: 508,
    suffix: "",
    label: "Real users",
    detail: "Proof of Concept in Mauritius & La Réunion",
  },
  {
    value: "15.11",
    numericValue: 15.11,
    suffix: "%",
    label: "Digital-to-IRL conversion",
    detail: "No other platform tracks this metric",
  },
  {
    value: "100",
    numericValue: 100,
    suffix: "%",
    label: "Sold-out events",
    detail: "Every Âme Intense event fully booked",
  },
];

// ─── Pricing ─────────────────────────────────────────────────────────────────

export interface PricingTier {
  name: string;
  price: string;
  priceNote: string;
  userLimit: string;
  features: string[];
  featured: boolean;
  cta: string;
}

export const PRICING_TIERS: PricingTier[] = [
  {
    name: "Starter",
    price: "$990",
    priceNote: "/month",
    userLimit: "Up to 5,000 users",
    features: [
      "Conversation Health Agent",
      "Know Me Preference Engine",
      "Basic analytics dashboard",
      "Email support",
      "API access",
    ],
    featured: false,
    cta: "Get Started",
  },
  {
    name: "Growth",
    price: "$3,900",
    priceNote: "/month",
    userLimit: "Up to 50,000 users",
    features: [
      "All 5 AI agents",
      "Safe Proximity engine",
      "Full analytics dashboard",
      "Dedicated success manager",
      "Priority API access",
      "Custom integration support",
    ],
    featured: true,
    cta: "Get Started",
  },
  {
    name: "Scale",
    price: "Custom",
    priceNote: "",
    userLimit: "Unlimited users",
    features: [
      "Everything in Growth",
      "Custom model fine-tuning",
      "White-label deployment",
      "99.9% SLA guarantee",
      "24/7 dedicated support",
      "On-premise option",
    ],
    featured: false,
    cta: "Contact Sales",
  },
];

// ─── Navigation ──────────────────────────────────────────────────────────────

export interface NavLink {
  label: string;
  href: string;
}

export const NAV_LINKS: NavLink[] = [
  { label: "How It Works", href: "/how-it-works" },
  { label: "Industries", href: "/industries" },
  { label: "The Whisper", href: "/the-whisper" },
  { label: "Proximity", href: "/proximity" },
  { label: "Developers", href: "/developers" },
  { label: "About", href: "/about" },
];

// ─── Footer ──────────────────────────────────────────────────────────────────

export interface FooterColumn {
  title: string;
  links: NavLink[];
}

export const FOOTER_COLUMNS: FooterColumn[] = [
  {
    title: "Product",
    links: [
      { label: "How It Works", href: "/how-it-works" },
      { label: "The Whisper", href: "/the-whisper" },
      { label: "Proximity", href: "/proximity" },
      { label: "Pricing", href: "/#pricing" },
    ],
  },
  {
    title: "Industries",
    links: [
      { label: "Dating & Social", href: "/industries#dating" },
      { label: "HR & Recruitment", href: "/industries#hr" },
      { label: "Wellness & Therapy", href: "/industries#wellness" },
      { label: "Professional Networking", href: "/industries#networking" },
      { label: "Enterprise & Teams", href: "/industries#enterprise" },
      { label: "Education & Mentorship", href: "/industries#education" },
    ],
  },
  {
    title: "Developers",
    links: [
      { label: "API Docs", href: "/developers" },
      { label: "Quick Start", href: "/developers#quickstart" },
      { label: "SDKs", href: "/developers#sdks" },
      { label: "Changelog", href: "/developers#changelog" },
    ],
  },
  {
    title: "Company",
    links: [
      { label: "About", href: "/about" },
      { label: "Blog", href: "#" },
      { label: "Press", href: "#" },
      { label: "Waitlist", href: "/waitlist" },
    ],
  },
];

// ─── API Examples (Developers page) ─────────────────────────────────────────

export interface ApiEndpoint {
  method: "GET" | "POST" | "PUT" | "DELETE";
  path: string;
  description: string;
  requestBody?: string;
  responseBody: string;
}

export const API_ENDPOINTS: ApiEndpoint[] = [
  {
    method: "POST",
    path: "/api/v1/compatibility",
    description: "Get compatibility score between two users",
    requestBody: `{
  "user_a": "usr_8f3k2j",
  "user_b": "usr_9d4m1n",
  "include_dimensions": true
}`,
    responseBody: `{
  "score": 0.84,
  "confidence": 0.92,
  "dimensions": {
    "communication_style": 0.91,
    "values_alignment": 0.87,
    "emotional_intelligence": 0.79,
    "lifestyle_compatibility": 0.82
  },
  "conversation_starters": [
    "You both value deep conversations over small talk",
    "Shared interest in creative pursuits"
  ]
}`,
  },
  {
    method: "GET",
    path: "/api/v1/feed",
    description: "Get personalized ranked feed for a user",
    responseBody: `{
  "user_id": "usr_8f3k2j",
  "feed": [
    {
      "match_id": "usr_9d4m1n",
      "score": 0.84,
      "reason": "High values alignment + complementary communication styles",
      "optimal_contact_time": "2026-03-30T18:30:00Z"
    }
  ],
  "generated_at": "2026-03-30T14:22:00Z"
}`,
  },
  {
    method: "POST",
    path: "/api/v1/proximity/location",
    description: "Update user location hash for proximity matching",
    requestBody: `{
  "user_id": "usr_8f3k2j",
  "geohash": "mk2g7d",
  "active": true
}`,
    responseBody: `{
  "status": "updated",
  "nearby_matches": 3,
  "highest_score": 0.84
}`,
  },
  {
    method: "GET",
    path: "/api/v1/knowme/questions",
    description: "Get today's Know Me questions for a user",
    responseBody: `{
  "user_id": "usr_8f3k2j",
  "questions": [
    {
      "id": "q_247",
      "dimension": "communication_style",
      "option_a": "I prefer to process my thoughts before responding",
      "option_b": "I think best while talking things through"
    },
    {
      "id": "q_118",
      "dimension": "values",
      "option_a": "Stability and predictability give me peace",
      "option_b": "I thrive on change and new experiences"
    }
  ],
  "completed": 94,
  "total": 138
}`,
  },
  {
    method: "POST",
    path: "/api/v1/whisper/suggest",
    description: "Get contextual conversation suggestion",
    requestBody: `{
  "conversation_id": "conv_4k8m2",
  "user_id": "usr_8f3k2j",
  "context_window": 5
}`,
    responseBody: `{
  "whisper": {
    "suggestion": "She mentioned not going back to her hometown much — her answers suggest this means something to her. This could be worth exploring gently.",
    "confidence": 0.88,
    "source": "cross_reference",
    "dimension": "emotional_depth",
    "dismissible": true
  }
}`,
  },
];

export const CODE_EXAMPLES: Record<string, Record<string, string>> = {
  compatibility: {
    javascript: `import EloDtx from '@elodtx/sdk';

const client = new EloDtx({
  apiKey: process.env.ELODTX_API_KEY,
});

const result = await client.compatibility.check({
  userA: 'usr_8f3k2j',
  userB: 'usr_9d4m1n',
  includeDimensions: true,
});

console.log(result.score);        // 0.84
console.log(result.dimensions);   // { communication_style: 0.91, ... }`,
    python: `from elodtx import EloDtx

client = EloDtx(api_key=os.environ["ELODTX_API_KEY"])

result = client.compatibility.check(
    user_a="usr_8f3k2j",
    user_b="usr_9d4m1n",
    include_dimensions=True,
)

print(result.score)        # 0.84
print(result.dimensions)   # {"communication_style": 0.91, ...}`,
    swift: `import EloDtx

let client = EloDtx(apiKey: ProcessInfo.processInfo.environment["ELODTX_API_KEY"]!)

let result = try await client.compatibility.check(
    userA: "usr_8f3k2j",
    userB: "usr_9d4m1n",
    includeDimensions: true
)

print(result.score)        // 0.84
print(result.dimensions)   // ["communication_style": 0.91, ...]`,
  },
};

// ─── Rate Limits ─────────────────────────────────────────────────────────────

export interface RateLimit {
  tier: string;
  requestsPerMinute: number;
  requestsPerDay: number;
  burstLimit: number;
}

export const RATE_LIMITS: RateLimit[] = [
  { tier: "Starter", requestsPerMinute: 60, requestsPerDay: 10000, burstLimit: 100 },
  { tier: "Growth", requestsPerMinute: 300, requestsPerDay: 100000, burstLimit: 500 },
  { tier: "Scale", requestsPerMinute: 1000, requestsPerDay: 1000000, burstLimit: 2000 },
];

// ─── Conversation Demo Data ──────────────────────────────────────────────────

export interface ChatMessage {
  sender: "user" | "match";
  text: string;
  timestamp: string;
}

export interface WhisperMessage {
  text: string;
  afterMessageIndex: number;
}

export const CONVERSATION_WITHOUT_WHISPER: ChatMessage[] = [
  { sender: "match", text: "Hey! I saw we matched. How's your day going?", timestamp: "2:14 PM" },
  { sender: "user", text: "Hey! Pretty good, just got back from a run. You?", timestamp: "2:16 PM" },
  { sender: "match", text: "Nice! I'm just relaxing at home. Do you run a lot?", timestamp: "2:18 PM" },
  { sender: "user", text: "Yeah, a few times a week. Keeps me sane haha", timestamp: "2:20 PM" },
  { sender: "match", text: "That's cool!", timestamp: "2:25 PM" },
];

export const CONVERSATION_WITH_WHISPER: ChatMessage[] = [
  { sender: "match", text: "Hey! I saw we matched. How's your day going?", timestamp: "2:14 PM" },
  { sender: "user", text: "Hey! Pretty good, just got back from a run. You?", timestamp: "2:16 PM" },
  { sender: "match", text: "Nice! I'm just relaxing at home. Do you run a lot?", timestamp: "2:18 PM" },
  { sender: "user", text: "Yeah, a few times a week. Keeps me sane haha", timestamp: "2:20 PM" },
  { sender: "match", text: "That's cool! I've been thinking about starting but I never know where to begin", timestamp: "2:22 PM" },
  { sender: "user", text: "I actually started because I needed to clear my head after moving cities. It became my way of exploring new places. Where did you grow up?", timestamp: "2:25 PM" },
  { sender: "match", text: "I grew up in a small town in the south. Haven't been back in a while actually... it's complicated", timestamp: "2:28 PM" },
  { sender: "user", text: "I get that. Sometimes distance from home gives you perspective you didn't know you needed. What brought you here?", timestamp: "2:31 PM" },
  { sender: "match", text: "That's exactly it. I came for work but stayed because I finally felt like I could be myself. Would you want to grab coffee sometime and swap stories?", timestamp: "2:35 PM" },
  { sender: "user", text: "I'd really like that. There's a great spot near the waterfront — Saturday morning?", timestamp: "2:37 PM" },
];

export const WHISPER_SUGGESTIONS: WhisperMessage[] = [
  {
    text: "She mentioned wanting to start running — your Know Me profile shows you value sharing activities. This could be a genuine connection point.",
    afterMessageIndex: 3,
  },
  {
    text: "She mentioned not going back to her hometown much — her answers suggest this means something to her. This could be worth exploring gently.",
    afterMessageIndex: 6,
  },
];

// ─── Proximity FAQ ───────────────────────────────────────────────────────────

export interface FAQ {
  question: string;
  answer: string;
}

export const PROXIMITY_FAQ: FAQ[] = [
  {
    question: "Can EloDtx track my exact location?",
    answer:
      "No. EloDtx uses geohash encoding that reduces your location to a ~500m × 500m area. Your exact coordinates are never stored, transmitted, or visible to anyone — including EloDtx servers. The system only knows your approximate area, never your precise position.",
  },
  {
    question: "Who can see that I'm nearby?",
    answer:
      "Only users who you've already matched with at a high compatibility score AND who have also opted into proximity. Both parties must have proximity enabled. You're invisible to everyone else.",
  },
  {
    question: "What data is stored?",
    answer:
      "Only the geohash (area code) and a timestamp. This data is ephemeral — it's deleted within 24 hours. No location history is ever built. No movement patterns are tracked.",
  },
  {
    question: "Can I turn it off?",
    answer:
      "Instantly. One tap disables proximity and immediately removes your geohash from the system. There's no cooldown, no 'are you sure' — it's off when you say it's off.",
  },
  {
    question: "Why Apple Watch?",
    answer:
      "A haptic tap on the wrist is the most discreet way to receive a proximity alert. It doesn't require pulling out your phone, doesn't make a sound, and feels natural rather than technological. It's designed to feel like intuition, not notification.",
  },
  {
    question: "What if I don't want to meet?",
    answer:
      "Every proximity alert has two buttons: 'Connect' and 'Not Now'. Tapping 'Not Now' dismisses the alert permanently for that encounter. No explanation needed. No notification sent to the other person. They never know you were alerted.",
  },
];

// ─── SEO Metadata ────────────────────────────────────────────────────────────

export const SEO = {
  siteName: "EloDtx",
  siteUrl: "https://elodtx.com",
  defaultTitle: "EloDtx — The AI Connection Intelligence Platform",
  defaultDescription:
    "Purpose-built AI that understands, predicts and improves human connection. 138 dimensions. 5 specialized agents. Real results.",
  pages: {
    home: {
      title: "EloDtx — The AI Connection Intelligence Platform",
      description:
        "Purpose-built AI that understands, predicts and improves human connection. 138 dimensions. 5 agents. Real results.",
    },
    howItWorks: {
      title: "How EloDtx Works — Whispers, Proximity & Behavioral AI",
      description:
        "The Whisper System, Safe Proximity, and the behavior learning loop explained. See how EloDtx connects the right people.",
    },
    industries: {
      title: "Industries — EloDtx for Dating, HR, Wellness & More",
      description:
        "Connection intelligence for dating, recruitment, wellness, networking, enterprise and education platforms.",
    },
    theWhisper: {
      title: "The Whisper — EloDtx Never Speaks For You",
      description:
        "EloDtx whispers contextual suggestions so you never miss a moment. See how it transforms conversations.",
    },
    proximity: {
      title: "Safe Proximity — Serendipity Powered by Intelligence",
      description:
        "Real-time compatibility scoring with geohash privacy. Apple Watch integration. Privacy-first real-world connection.",
    },
    developers: {
      title: "EloDtx API Documentation — Quick Start & SDKs",
      description:
        "REST API docs, code examples in JavaScript, Python, and Swift. Rate limits, SDKs, and interactive pricing calculator.",
    },
    about: {
      title: "About EloDtx — Built in Mauritius for the World",
      description:
        "The origin story, technology, team, and investment behind the AI Connection Intelligence Platform.",
    },
    waitlist: {
      title: "Join the EloDtx Waitlist — First 10 Partners Get 90 Days Free",
      description:
        "Be among the first platforms powered by EloDtx. Apply for early access with full dedicated support.",
    },
  },
};

// ─── Training Path (How It Works page) ───────────────────────────────────────

export interface TrainingPhase {
  period: string;
  title: string;
  description: string;
  status: "current" | "upcoming" | "future";
}

export const TRAINING_PATH: TrainingPhase[] = [
  {
    period: "Now",
    title: "Claude API Layer",
    description:
      "EloDtx runs on Anthropic's Claude API with proprietary prompting, fine-tuned for connection intelligence. This gives us production-grade AI while we build our own.",
    status: "current",
  },
  {
    period: "Month 12–18",
    title: "Qwen 3.5 Fine-Tuning",
    description:
      "Fine-tuning Qwen 3.5 on our proprietary behavioral dataset — 508 real user profiles, conversation patterns, and outcome data from the Baeyond POC.",
    status: "upcoming",
  },
  {
    period: "Month 18–24",
    title: "Hybrid Deployment",
    description:
      "Running our fine-tuned model alongside Claude for validation. Gradual traffic migration as our model proves equal or superior performance.",
    status: "future",
  },
  {
    period: "Month 24+",
    title: "Full Proprietary EloDtx Model",
    description:
      "Complete independence. A model trained exclusively on connection data, optimized with TurboQuant compression for 4.9x efficiency.",
    status: "future",
  },
];

// ─── About Page Data ─────────────────────────────────────────────────────────

export interface TimelineEvent {
  year: string;
  title: string;
  description: string;
}

export const ABOUT_TIMELINE: TimelineEvent[] = [
  {
    year: "2024",
    title: "Baeyond is born",
    description:
      "A dating platform built in Mauritius with a radical idea: what if matching was based on who people actually are, not what they look like?",
  },
  {
    year: "2024",
    title: "Âme Intense launches",
    description:
      "Real-world connection events in Mauritius and La Réunion. Every single event sells out. 508 real users prove the concept.",
  },
  {
    year: "2025",
    title: "EloDtx is extracted",
    description:
      "The AI engine behind Baeyond is too powerful to live in one app. EloDtx becomes a standalone platform — connection intelligence as a service.",
  },
  {
    year: "2025",
    title: "$7.5M investment secured",
    description:
      "Alice Rose LLC (USA) backs the vision. Urban Space Web Technologies Ltd (Mauritius) and Eloclouds Limited (UK) formalize the partnership.",
  },
  {
    year: "2026",
    title: "Platform launch",
    description:
      "EloDtx opens to the first 10 partner platforms. The Âme Intense launch event in Mauritius, October 2026, marks the public debut.",
  },
];
```

- [ ] **Step 2: Verify the file compiles**

```bash
cd /c/Users/steph/elodtx-website
npx tsc --noEmit lib/constants.ts 2>&1 || echo "Will verify at build time"
```

- [ ] **Step 3: Commit**

```bash
cd /c/Users/steph/elodtx-website
git add lib/constants.ts
git commit -m "feat: add all site content data and type definitions"
```

---

## Task 3: Layout — Root Layout, Navbar, Footer

**Files:**
- Modify: `app/layout.tsx`
- Create: `components/layout/navbar.tsx`, `components/layout/footer.tsx`

- [ ] **Step 1: Create Navbar component**

Create `components/layout/navbar.tsx` — a `"use client"` component:
- Sticky (`fixed top-0 z-50`), full-width, `backdrop-blur-md` with `bg-dark-primary/80`
- Left: "EloDtx" logo text in `text-brand-gold font-bold text-xl` with a small SVG connection-node icon (two circles connected by a line)
- Center: `NAV_LINKS` mapped to `next/link` components, highlight active route using `usePathname()`
- Right: "Get Early Access" gold button linking to `/waitlist`
- Mobile: hamburger button (Lucide `Menu` / `X` icons) toggles a slide-down panel with all links
- Transition: mobile menu uses Framer Motion `AnimatePresence` with slide-down animation
- Nav links have the gold underline hover effect (animated `::after` pseudo-element via Tailwind)

- [ ] **Step 2: Create Footer component**

Create `components/layout/footer.tsx` — server component:
- Top section: EloDtx logo + tagline "The Connection Intelligence Platform"
- Middle: 4-column grid from `FOOTER_COLUMNS`, responsive (4 cols desktop, 2 tablet, 1 mobile)
- Social links row: LinkedIn, Twitter/X, GitHub icons (Lucide) — link to `#` for now
- Bottom bar: copyright "© 2026 Urban Space Web Technologies Ltd · Mauritius · Built in partnership with Eloclouds Limited UK"
- Legal links: Privacy Policy | Terms of Service | GDPR (all link to `#`)
- Color scheme: `text-text-secondary` for links, `text-text-muted` for legal, `text-brand-gold` on hover

- [ ] **Step 3: Update app/layout.tsx**

Replace `app/layout.tsx`:
- Import Inter and JetBrains Mono via `next/font/google`
- Set `className` variables: `--font-inter` and `--font-jetbrains`
- Default metadata using `SEO.defaultTitle` and `SEO.defaultDescription`
- Body: `<Navbar />` + `<main className="min-h-screen pt-20">` (pt-20 for fixed nav offset) + `{children}` + `<Footer />`
- Add `className="dark"` to `<html>` tag
- Import `globals.css`

- [ ] **Step 4: Clear default page content**

Replace `app/page.tsx` with a minimal placeholder:

```tsx
export default function Home() {
  return (
    <div className="flex items-center justify-center min-h-[80vh]">
      <h1 className="text-4xl font-bold text-brand-gold">EloDtx</h1>
    </div>
  );
}
```

- [ ] **Step 5: Verify dev server runs**

```bash
cd /c/Users/steph/elodtx-website
npm run dev
```

Open http://localhost:3000 — should see dark background, gold "EloDtx" text, working navbar and footer.

- [ ] **Step 6: Commit**

```bash
cd /c/Users/steph/elodtx-website
git add app/layout.tsx app/page.tsx components/layout/
git commit -m "feat: add root layout with navbar and footer"
```

---

## Task 4: Shared Components — SectionReveal, AnimatedCounter, CompatibilityScore

**Files:**
- Create: `components/shared/section-reveal.tsx`, `components/shared/animated-counter.tsx`, `components/shared/compatibility-score.tsx`

- [ ] **Step 1: Create SectionReveal**

Create `components/shared/section-reveal.tsx` — `"use client"`:
- Props: `direction?: "up" | "left" | "right"` (default `"up"`), `delay?: number` (default `0`), `className?: string`, `children: React.ReactNode`
- Uses `framer-motion`'s `motion.div` with `useInView` (ref-based, `once: true`, `margin: "-100px"`)
- Initial state: `opacity: 0` + translate based on direction (up: `y: 30`, left: `x: -30`, right: `x: 30`)
- Animate to: `opacity: 1, x: 0, y: 0` with `duration: 0.6, ease: "easeOut"`, respecting `delay`

- [ ] **Step 2: Create AnimatedCounter**

Create `components/shared/animated-counter.tsx` — `"use client"`:
- Props: `target: number`, `suffix?: string`, `prefix?: string`, `duration?: number` (default `1.5`), `className?: string`
- Uses `useInView` to trigger, `useMotionValue` + `useTransform` + `animate` from Framer Motion
- On view: animates from 0 to `target` over `duration` seconds
- Displays: `prefix` + rounded value + `suffix`
- Handles decimals: if `target` has decimal places, display with same precision

- [ ] **Step 3: Create CompatibilityScore**

Create `components/shared/compatibility-score.tsx` — `"use client"`:
- Props: `score: number` (0-100), `size?: "sm" | "md" | "lg"` (default `"md"`), `animated?: boolean` (default `true`), `className?: string`
- SVG circle with stroke-dasharray animation
- Sizes: sm=80px, md=120px, lg=160px
- Background circle: `stroke: var(--dark-tertiary)`
- Progress circle: `stroke: var(--brand-gold)`, animated from 0 to score percentage using Framer Motion
- Center text: score number with "%" suffix, animated counter
- Uses `useInView` to trigger animation (once)

- [ ] **Step 4: Commit**

```bash
cd /c/Users/steph/elodtx-website
git add components/shared/
git commit -m "feat: add SectionReveal, AnimatedCounter, and CompatibilityScore components"
```

---

## Task 5: Shared Components — AnimatedConnectionGraph

**Files:**
- Create: `components/shared/animated-connection-graph.tsx`

- [ ] **Step 1: Create AnimatedConnectionGraph**

Create `components/shared/animated-connection-graph.tsx` — `"use client"`:
- Props: `density?: number` (default `50`, number of nodes), `speed?: number` (default `0.5`), `color?: string` (default `"#C9A84C"`), `className?: string`
- Uses HTML Canvas via `useRef<HTMLCanvasElement>` and `useEffect` for animation loop
- Full-width, full-height (`absolute inset-0`) with pointer-events-none
- Nodes: small circles (radius 2-4px) at random positions, floating with sine/cosine movement
- Edges: lines drawn between nodes within a distance threshold (150px), opacity inversely proportional to distance
- Node color: the `color` prop at 40% opacity
- Edge color: the `color` prop at 15% opacity
- Animation: `requestAnimationFrame` loop, each node has random dx/dy velocity scaled by `speed`
- Nodes wrap around canvas edges
- Canvas resizes with window (`ResizeObserver`)
- Cleanup: cancel animation frame on unmount

- [ ] **Step 2: Commit**

```bash
cd /c/Users/steph/elodtx-website
git add components/shared/animated-connection-graph.tsx
git commit -m "feat: add AnimatedConnectionGraph canvas component"
```

---

## Task 6: Shared Components — AgentCard, ConversationDemo, WaitlistForm

**Files:**
- Create: `components/shared/agent-card.tsx`, `components/shared/conversation-demo.tsx`, `components/shared/waitlist-form.tsx`

- [ ] **Step 1: Create AgentCard**

Create `components/shared/agent-card.tsx` — `"use client"`:
- Props: `agent: Agent` (from constants), `index?: number` (for stagger delay)
- Card with `bg-dark-secondary` background, `border border-dark-tertiary`, rounded-xl, padding
- Top: agent icon (Lucide component) in gold, agent name as h3, subtitle as muted text
- Default state: shows `shortDescription`
- Expanded state (hover on desktop, click on mobile): shows `fullDescription` with Framer Motion `AnimatePresence` height animation
- Hover effect: `translateY(-4px)` + gold border glow (`gold-glow` utility)
- Uses `motion.div` for the expand/collapse animation
- Mobile detection: use a `useState` toggle on click instead of hover

- [ ] **Step 2: Create ConversationDemo**

Create `components/shared/conversation-demo.tsx` — `"use client"`:
- Props: `withWhisper: boolean`, `autoPlay?: boolean` (default `false`), `className?: string`
- Phone frame: rounded-2xl border with `bg-dark-secondary`, max-w-sm, styled like a messaging app
- Header bar: "Baeyond Messenger" title, faux status bar
- Messages area: scrollable div
- Uses data from `CONVERSATION_WITHOUT_WHISPER` or `CONVERSATION_WITH_WHISPER` based on `withWhisper` prop
- Messages animate in sequentially (Framer Motion stagger, 1.5s between)
- User messages: right-aligned, `bg-brand-gold/20` with gold border
- Match messages: left-aligned, `bg-dark-tertiary`
- If `withWhisper`: after the message at `WHISPER_SUGGESTIONS[n].afterMessageIndex`, show a golden whisper card — `bg-brand-gold/10 border border-brand-gold/30` with sparkle icon and the whisper text. Dismissible with X button.
- Play button: if not `autoPlay`, show a "Play Conversation" button that triggers the sequential animation
- Timestamps shown below each message in `text-text-muted text-xs`

- [ ] **Step 3: Create WaitlistForm**

Create `components/shared/waitlist-form.tsx` — `"use client"`:
- Props: `compact?: boolean` (default `false`)
- **Compact mode** (used in FinalCTA): single email input + submit button in a row. Gold gradient button.
- **Full mode** (used on /waitlist page): all fields in a form:
  - Name (text input, required)
  - Company (text input, required)
  - Email (email input, required, validated with regex)
  - Platform type (select dropdown: Dating / HR / Wellness / Networking / Enterprise / Education / Other)
  - Monthly active users (select dropdown: Under 5,000 / 5,000–50,000 / 50,000–500,000 / 500,000+)
  - Message (textarea, optional)
- State machine: `idle` | `submitting` | `success` | `error` (use `useState`)
- On submit: POST to `/api/waitlist` with JSON body. Show spinner during `submitting`.
- Success state: replace form with "Thank you! We'll be in touch within 48 hours." message with a subtle gold checkmark animation
- Error state: show error message below form with retry button
- All inputs styled with `bg-dark-secondary border-dark-tertiary` focus ring in gold
- Use shadcn/ui `Button`, `Input`, `Textarea`, `Select` components

- [ ] **Step 4: Commit**

```bash
cd /c/Users/steph/elodtx-website
git add components/shared/agent-card.tsx components/shared/conversation-demo.tsx components/shared/waitlist-form.tsx
git commit -m "feat: add AgentCard, ConversationDemo, and WaitlistForm components"
```

---

## Task 7: Shared Components — ProximityAlertMockup, PricingCalculator, CodeBlock

**Files:**
- Create: `components/shared/proximity-alert-mockup.tsx`, `components/shared/pricing-calculator.tsx`, `components/shared/code-block.tsx`

- [ ] **Step 1: Create ProximityAlertMockup**

Create `components/shared/proximity-alert-mockup.tsx` — `"use client"`:
- Props: `score?: number` (default `84`), `traits?: string[]` (default `["Creative pursuits", "Deep conversations", "Morning person"]`), `distance?: string` (default `"~150m"`), `className?: string`
- Apple Watch frame: rounded-[44px] border, ~180px wide, aspect ratio matching Apple Watch
- Watch body: `bg-dark-secondary` with subtle metallic border
- Animation on scroll: watch "vibrates" (3x quick horizontal shake, 2px amplitude, using Framer Motion keyframes), then alert card slides up from bottom
- Alert card content (5 elements):
  1. CompatibilityScore circle (small size) showing `score`%
  2. Shared traits: list of `traits` as small gold badges
  3. Activity: "Having coffee" (static text with coffee icon)
  4. Distance: `distance` shown with map-pin icon
  5. Two buttons: "Connect" (gold filled, small) and "Not Now" (outline, small)
- Overall: dark card on dark watch face, gold accents

- [ ] **Step 2: Create PricingCalculator**

Create `components/shared/pricing-calculator.tsx` — `"use client"`:
- Props: `className?: string`
- Slider input: range 1000 to 500000, logarithmic scale (use `Math.log` for position mapping)
- Display: current MAU count formatted with commas
- Below slider: recommended tier name, monthly cost, and cost-per-user
- Logic:
  - 1K–5K: Starter, $990/mo, cost = 990/MAU
  - 5K–50K: Growth, $3,900/mo, cost = 3900/MAU
  - 50K+: Scale, "Custom pricing — contact sales"
- Styled range input: custom thumb in gold, track in `dark-tertiary` with gold fill
- Use CSS for custom range styling (`::-webkit-slider-thumb`, etc.)
- Results displayed in a card below the slider

- [ ] **Step 3: Create CodeBlock**

Create `components/shared/code-block.tsx` — `"use client"`:
- Props: `code: string`, `language: string`, `className?: string`; OR `tabs: { language: string; label: string; code: string }[]` for multi-language
- If `tabs` provided: render language selector buttons at top (JS / Python / Swift), active tab highlighted in gold
- Code area: `bg-dark-secondary` with `rounded-lg`, overflow-x-auto, `font-mono text-sm`
- Syntax highlighting: use `shiki` with a dark theme (e.g., `"github-dark"`) — load the highlighter with `useEffect` and render highlighted HTML via `dangerouslySetInnerHTML`
- Copy button: top-right, copies code to clipboard. Shows checkmark for 2 seconds after copy.
- Fallback: if shiki hasn't loaded yet, show plain text in `<pre>` tag

- [ ] **Step 4: Commit**

```bash
cd /c/Users/steph/elodtx-website
git add components/shared/proximity-alert-mockup.tsx components/shared/pricing-calculator.tsx components/shared/code-block.tsx
git commit -m "feat: add ProximityAlertMockup, PricingCalculator, and CodeBlock components"
```

---

## Task 8: Home Page — Hero, StatsStrip, HowItWorks

**Files:**
- Create: `components/home/hero.tsx`, `components/home/stats-strip.tsx`, `components/home/how-it-works.tsx`
- Modify: `app/page.tsx`

- [ ] **Step 1: Create Hero component**

Create `components/home/hero.tsx` — `"use client"`:
- Full viewport height minus nav (`min-h-[calc(100vh-5rem)]`), relative position
- Background: `<AnimatedConnectionGraph />` as absolute-positioned child
- Gradient overlay: `absolute inset-0 bg-gradient-to-b from-dark-primary/50 via-transparent to-dark-primary`
- Content (centered, z-10, max-w-4xl mx-auto, text-center):
  - Headline: "The AI that understands human connection." — each word wrapped in `motion.span` with stagger animation (0.08s between, fade up from 20px)
  - Subheading: the full paragraph from spec, `text-xl text-text-secondary`, fades in after headline completes (0.5s delay)
  - Two buttons side by side (flex gap-4, centered):
    - "Get Early Access" — `gold-gradient` background, white text, rounded-lg, px-8 py-3, hover scale 1.02
    - "See How It Works" — outline with `border-brand-gold text-brand-gold`, hover bg-brand-gold/10
  - Both animate in with fade-up after subheading

- [ ] **Step 2: Create StatsStrip component**

Create `components/home/stats-strip.tsx` — `"use client"`:
- Background: `bg-dark-secondary/50` with top/bottom border in `dark-tertiary`
- Desktop: 6 columns grid with dividers between
- Mobile: horizontal scroll (`overflow-x-auto flex gap-8`, snap scroll)
- Each stat: `<AnimatedCounter>` with large number + suffix/prefix, small label below in `text-text-secondary`
- Wrapped in `<SectionReveal>`

- [ ] **Step 3: Create HowItWorks component**

Create `components/home/how-it-works.tsx` — `"use client"`:
- Section heading: "How It Works" (h2, centered)
- Desktop: horizontal layout with 5 steps connected by lines/arrows
- Mobile: vertical timeline (line on left, steps stacked)
- Each step: icon in gold circle, title (h3), description paragraph
- Steps animate in with stagger (0.15s delay between) using `<SectionReveal>`
- Connecting lines: thin `bg-dark-tertiary` lines between steps, with small gold dots at intersections

- [ ] **Step 4: Wire up in app/page.tsx**

Update `app/page.tsx`:

```tsx
import { Hero } from "@/components/home/hero";
import { StatsStrip } from "@/components/home/stats-strip";
import { HowItWorks } from "@/components/home/how-it-works";

export default function Home() {
  return (
    <>
      <Hero />
      <StatsStrip />
      <HowItWorks />
    </>
  );
}
```

- [ ] **Step 5: Verify in browser**

```bash
cd /c/Users/steph/elodtx-website
npm run dev
```

Check: hero animation plays, stats count up on scroll, how-it-works steps stagger in.

- [ ] **Step 6: Commit**

```bash
cd /c/Users/steph/elodtx-website
git add components/home/hero.tsx components/home/stats-strip.tsx components/home/how-it-works.tsx app/page.tsx
git commit -m "feat: add Hero, StatsStrip, and HowItWorks home sections"
```

---

## Task 9: Home Page — Agents, Industries, Comparison

**Files:**
- Create: `components/home/agents-section.tsx`, `components/home/industries-section.tsx`, `components/home/comparison-table.tsx`
- Modify: `app/page.tsx`

- [ ] **Step 1: Create AgentsSection**

Create `components/home/agents-section.tsx` — `"use client"`:
- Section heading: "Five Specialized Agents" (h2, centered), subheading: "Each one purpose-built for a specific dimension of human connection."
- Grid: 3 columns desktop (first row 3, second row 2 centered), 2 columns tablet, 1 column mobile
- Maps `AGENTS` array to `<AgentCard>` components with stagger index
- Wrapped in `<SectionReveal>`

- [ ] **Step 2: Create IndustriesSection**

Create `components/home/industries-section.tsx` — `"use client"`:
- Section heading: "Built for Every Industry That Connects People" (h2, centered)
- Uses shadcn/ui `<Tabs>` component
- Tab triggers: 6 industry names with their Lucide icons, horizontal scroll on mobile
- Tab content: for each industry, show `tagline` as large text, `description`, then `features` as a 2-column grid of feature cards (title + description)
- Content transitions: `AnimatePresence` with `mode="wait"` for crossfade between tabs
- Active tab trigger highlighted with gold underline

- [ ] **Step 3: Create ComparisonTable**

Create `components/home/comparison-table.tsx` — `"use client"`:
- Section heading: "How EloDtx Compares" (h2, centered)
- Responsive table inside a `rounded-xl border border-dark-tertiary overflow-hidden`
- Header row: Capability | General AI (GPT/Gemini) | Standard Algorithms | EloDtx
- EloDtx column header has gold text
- Body rows from `COMPARISON_DATA`:
  - "yes": green checkmark icon (Lucide `Check`) in a green circle
  - "no": red X icon (Lucide `X`) in a muted circle
  - "partial": yellow/amber dash icon
  - EloDtx column cells have subtle `bg-brand-gold/5` background
- Rows animate in with stagger (0.08s) using Framer Motion
- Mobile: horizontally scrollable with sticky first column (`position: sticky, left: 0`)

- [ ] **Step 4: Update app/page.tsx**

Add imports and components after HowItWorks:

```tsx
import { AgentsSection } from "@/components/home/agents-section";
import { IndustriesSection } from "@/components/home/industries-section";
import { ComparisonTable } from "@/components/home/comparison-table";
```

Add `<AgentsSection />`, `<IndustriesSection />`, `<ComparisonTable />` after `<HowItWorks />`.

- [ ] **Step 5: Commit**

```bash
cd /c/Users/steph/elodtx-website
git add components/home/agents-section.tsx components/home/industries-section.tsx components/home/comparison-table.tsx app/page.tsx
git commit -m "feat: add Agents, Industries, and Comparison home sections"
```

---

## Task 10: Home Page — Proof, Pricing, FinalCTA

**Files:**
- Create: `components/home/proof-section.tsx`, `components/home/pricing-section.tsx`, `components/home/final-cta.tsx`
- Modify: `app/page.tsx`

- [ ] **Step 1: Create ProofSection**

Create `components/home/proof-section.tsx` — `"use client"`:
- Section heading: "Proven with Real People" (h2, centered)
- 3 metric cards in a row (grid, 3 col desktop, stack mobile):
  - Each card: `bg-dark-secondary rounded-xl p-8 border border-dark-tertiary`
  - Large `<AnimatedCounter>` for the number
  - Label in `text-brand-gold font-medium`
  - Detail text in `text-text-secondary text-sm`
- Below cards: testimonial-style block — large quote text in `text-xl italic text-text-secondary` with gold left border. Quote: "We didn't build EloDtx in a lab. We built it in the real world, with real people, and measured what actually happened."
- Attribution: "— Baeyond Proof of Concept, Mauritius & La Réunion"
- All wrapped in `<SectionReveal>`

- [ ] **Step 2: Create PricingSection**

Create `components/home/pricing-section.tsx` — `"use client"`:
- `id="pricing"` for anchor link
- Section heading: "Simple, Transparent Pricing" (h2, centered)
- 3 pricing cards from `PRICING_TIERS`:
  - Each card: `bg-dark-secondary rounded-xl border` with padding
  - Name at top, price in large text, priceNote smaller
  - userLimit as badge
  - Features as checkmark list
  - CTA button at bottom
  - **Featured card** (Growth): `border-brand-gold gold-glow` + `scale-105` + "Most Popular" badge at top in gold
  - Non-featured: `border-dark-tertiary`
- Cards animate in with scale (0.9→1) and stagger, middle card delayed 0.1s
- Responsive: 3-col desktop, stack mobile

- [ ] **Step 3: Create FinalCTA**

Create `components/home/final-cta.tsx` — `"use client"`:
- Full-width section with `bg-dark-secondary/30`
- Centered content, max-w-2xl:
  - Headline: "Join the waitlist. First 10 partners get 90 days free." in h2
  - Subheading: "Be among the first platforms powered by connection intelligence."
  - `<WaitlistForm compact />` below
- Wrapped in `<SectionReveal>`

- [ ] **Step 4: Update app/page.tsx**

Add imports and components. Final page.tsx order:
Hero → StatsStrip → HowItWorks → AgentsSection → IndustriesSection → ComparisonTable → ProofSection → PricingSection → FinalCTA

- [ ] **Step 5: Add SEO metadata to page**

Add to top of `app/page.tsx`:

```tsx
import { SEO } from "@/lib/constants";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: SEO.pages.home.title,
  description: SEO.pages.home.description,
  openGraph: {
    title: SEO.pages.home.title,
    description: SEO.pages.home.description,
    siteName: SEO.siteName,
    type: "website",
  },
  twitter: {
    card: "summary_large_image",
    title: SEO.pages.home.title,
    description: SEO.pages.home.description,
  },
};
```

- [ ] **Step 6: Commit**

```bash
cd /c/Users/steph/elodtx-website
git add components/home/ app/page.tsx
git commit -m "feat: complete home page with all 9 sections and SEO metadata"
```

---

## Task 11: How It Works Page

**Files:**
- Create: `app/how-it-works/page.tsx`, `components/how-it-works/whisper-system-diagram.tsx`, `components/how-it-works/proximity-flow-steps.tsx`, `components/how-it-works/behavior-learning-loop.tsx`, `components/how-it-works/training-path-timeline.tsx`

- [ ] **Step 1: Create WhisperSystemDiagram**

Create `components/how-it-works/whisper-system-diagram.tsx` — `"use client"`:
- Section with heading "The Whisper System" and subheading "EloDtx never writes messages for users. It whispers suggestions."
- Left side: explanatory text covering the philosophy, example scenario (the hometown example from spec), and ephemeral processing explanation
- Right side: a simplified visual showing a chat message input with a golden whisper card above it, with labels pointing to "Visible only to you", "Based on weeks of behavioral data", "Combined with live conversation signals"
- Mobile: stack vertically
- All text in `<SectionReveal>`

- [ ] **Step 2: Create ProximityFlowSteps**

Create `components/how-it-works/proximity-flow-steps.tsx` — `"use client"`:
- Section heading: "The Proximity Flow"
- Vertical flow diagram with 7 connected steps:
  1. User enables proximity
  2. Geohash location sent (±500m, never exact)
  3. EloDtx runs compatibility check (<2 seconds)
  4. Apple Watch haptic tap
  5. 5-element alert card appears
  6. Profile request sent
  7. Consent → Messenger unlocks
- Each step: numbered circle (gold) + title + brief description, connected by vertical line
- Steps stagger in on scroll
- Include the `<ProximityAlertMockup>` inline at step 5

- [ ] **Step 3: Create BehaviorLearningLoop**

Create `components/how-it-works/behavior-learning-loop.tsx` — `"use client"`:
- Section heading: "The Behavior Learning Loop"
- Visual diagram (CSS/SVG based):
  - Left column: "Passive Signals" — scroll behavior, session timing, notification response (listed with subtle icons)
  - Right column: "Active Signals" — intents sent, messages, event attendance, Know Me answers
  - Both columns feed into center: "Dynamic Profile" shown as a glowing gold circle/node
  - Arrow from center to bottom: "Real-time updates" flowing back into the system
- Animate: signals flow inward (Framer Motion, staggered fade-in from sides toward center)

- [ ] **Step 4: Create TrainingPathTimeline**

Create `components/how-it-works/training-path-timeline.tsx` — `"use client"`:
- Section heading: "The Training Path"
- Horizontal timeline (desktop) / vertical timeline (mobile) showing `TRAINING_PATH` phases
- Each phase: period label, title, description
- Current phase: highlighted with gold accent and "Current" badge
- Upcoming/future: progressively more faded
- Connected by line with dots at each phase

- [ ] **Step 5: Create the page**

Create `app/how-it-works/page.tsx`:

```tsx
import type { Metadata } from "next";
import { SEO } from "@/lib/constants";
import { WhisperSystemDiagram } from "@/components/how-it-works/whisper-system-diagram";
import { ProximityFlowSteps } from "@/components/how-it-works/proximity-flow-steps";
import { BehaviorLearningLoop } from "@/components/how-it-works/behavior-learning-loop";
import { TrainingPathTimeline } from "@/components/how-it-works/training-path-timeline";

export const metadata: Metadata = {
  title: SEO.pages.howItWorks.title,
  description: SEO.pages.howItWorks.description,
  openGraph: {
    title: SEO.pages.howItWorks.title,
    description: SEO.pages.howItWorks.description,
    siteName: SEO.siteName,
  },
};

export default function HowItWorksPage() {
  return (
    <div className="py-20">
      <div className="max-w-site mx-auto px-6">
        <h1 className="text-4xl md:text-5xl font-bold text-center mb-4">
          How <span className="text-brand-gold">EloDtx</span> Works
        </h1>
        <p className="text-xl text-text-secondary text-center max-w-3xl mx-auto mb-20">
          From integration to insight — here's how connection intelligence transforms your platform.
        </p>
      </div>
      <WhisperSystemDiagram />
      <ProximityFlowSteps />
      <BehaviorLearningLoop />
      <TrainingPathTimeline />
    </div>
  );
}
```

- [ ] **Step 6: Commit**

```bash
cd /c/Users/steph/elodtx-website
git add app/how-it-works/ components/how-it-works/
git commit -m "feat: add How It Works page with all four sections"
```

---

## Task 12: Industries Page

**Files:**
- Create: `app/industries/page.tsx`

- [ ] **Step 1: Create the Industries page**

Create `app/industries/page.tsx`:
- SEO metadata from `SEO.pages.industries`
- Page heading: "Industries" with subheading
- For each industry in `INDUSTRIES`, render a full section (`id={industry.id}` for anchor links):
  - Industry icon + name as h2
  - Tagline in large gold text
  - Two-column layout (desktop):
    - Left: `problem` text, `problemStats` in a highlighted callout box (`bg-dark-secondary border-l-4 border-brand-gold`)
    - Right: `description`, then `features` as cards in a 2-col grid
  - Below: "Active Agents" shown as gold badges listing `activeAgents`
  - "Expected Outcomes" as a list with checkmark icons
  - If `caseStudy`: highlighted case study box with gold border
- Sections separated by subtle dividers
- Each section wrapped in `<SectionReveal>`
- Alternating sections: even sections have image/detail on left, odd on right (visual variety)

- [ ] **Step 2: Commit**

```bash
cd /c/Users/steph/elodtx-website
git add app/industries/
git commit -m "feat: add Industries page with all 6 verticals"
```

---

## Task 13: The Whisper Page

**Files:**
- Create: `app/the-whisper/page.tsx`, `components/the-whisper/conversation-comparison.tsx`, `components/the-whisper/whisper-sources-diagram.tsx`

- [ ] **Step 1: Create ConversationComparison**

Create `components/the-whisper/conversation-comparison.tsx` — `"use client"`:
- Side-by-side layout (desktop), stacked (mobile)
- Left: `<ConversationDemo withWhisper={false} />` with label "Without EloDtx" and a red-ish/muted overlay tint. After the last message, show a "Conversation died" indicator with a flatline metaphor.
- Right: `<ConversationDemo withWhisper={true} />` with label "With EloDtx" and subtle gold tint. After the last message, show "Coffee date scheduled ☕" success indicator.
- Both start playing when scrolled into view (or user clicks a single "Watch the difference" button that triggers both simultaneously)

- [ ] **Step 2: Create WhisperSourcesDiagram**

Create `components/the-whisper/whisper-sources-diagram.tsx` — `"use client"`:
- Three source cards flowing into a central "Whisper" output:
  1. "Know Me Profile" — icon: Brain — "What EloDtx inferred over weeks of behavioral observation"
  2. "Live Conversation Signals" — icon: MessageCircle — "What EloDtx detects in the current exchange (ephemerally, never stored)"
  3. "Cross-Reference Intelligence" — icon: Zap — "When a live signal matches a known depth dimension in the profile"
- Visual: three cards at top/sides, lines converging to a golden whisper card at bottom/center
- Animated: cards fade in with stagger, then lines draw in, then whisper card appears
- Below: "What EloDtx NEVER does" section — 4 items with X icons:
  - Never writes responses for users
  - Never shares profile data directly
  - Never stores message content
  - Never interferes without consent

- [ ] **Step 3: Create the page**

Create `app/the-whisper/page.tsx`:
- SEO metadata
- Page heading: "The Whisper" with subheading: "EloDtx never speaks for you. It makes sure you don't miss the moment."
- Philosophy section: paragraph explaining the core idea
- `<ConversationComparison />`
- `<WhisperSourcesDiagram />`
- UI description section: "The whisper appears as a subtle golden suggestion beneath the message input — dismissible with one tap." with a simple visual mockup of a text input with a golden card above it

- [ ] **Step 4: Commit**

```bash
cd /c/Users/steph/elodtx-website
git add app/the-whisper/ components/the-whisper/
git commit -m "feat: add The Whisper page with conversation demo and sources diagram"
```

---

## Task 14: Proximity Page

**Files:**
- Create: `app/proximity/page.tsx`, `components/proximity/geohash-privacy-diagram.tsx`, `components/proximity/consent-flow-diagram.tsx`, `components/proximity/proximity-faq.tsx`

- [ ] **Step 1: Create GeohashPrivacyDiagram**

Create `components/proximity/geohash-privacy-diagram.tsx` — `"use client"`:
- Visual showing a map grid (CSS grid of cells, abstractly representing city blocks)
- One cell highlighted in gold (the geohash area) with label "~500m × 500m"
- A dot somewhere inside the cell with label "Your actual position (never transmitted)"
- Annotation: "EloDtx only knows the gold zone. Not the dot."
- Animated: grid fades in, then gold cell highlights, then dot appears with annotation

- [ ] **Step 2: Create ConsentFlowDiagram**

Create `components/proximity/consent-flow-diagram.tsx` — `"use client"`:
- Vertical flow chart showing consent steps:
  1. "Both users opt into Proximity" →
  2. "High compatibility score confirmed" →
  3. "Both in same geohash area" →
  4. "Haptic alert sent" →
  5. "User taps 'Connect'" →
  6. "Profile request sent to match" →
  7. "Match accepts" →
  8. "Messenger unlocks"
- Each step has a gate/check icon. Steps 1-3 are labeled "Automatic", steps 5-8 are labeled "Requires consent"
- Animated stagger on scroll

- [ ] **Step 3: Create ProximityFAQ**

Create `components/proximity/proximity-faq.tsx` — `"use client"`:
- Accordion-style FAQ using `PROXIMITY_FAQ` data
- Each item: question as clickable header, answer expands below with Framer Motion height animation
- Gold chevron icon rotates on expand
- Only one item open at a time

- [ ] **Step 4: Create the page**

Create `app/proximity/page.tsx`:
- SEO metadata
- Hero-style top: vision statement ("You're walking down the street...") in large italic text
- `<ProximityAlertMockup />` centered below vision
- "How It Works" section with geohash explanation text + `<GeohashPrivacyDiagram />`
- Apple Watch section: description of haptic integration
- `<ConsentFlowDiagram />`
- "Serendipity, Not Surveillance" section: paragraph explaining why the combination of deep scoring + minimal footprint + haptic delivery creates serendipity
- `<ProximityFAQ />`

- [ ] **Step 5: Commit**

```bash
cd /c/Users/steph/elodtx-website
git add app/proximity/ components/proximity/
git commit -m "feat: add Proximity page with privacy diagram, consent flow, and FAQ"
```

---

## Task 15: Developers Page

**Files:**
- Create: `app/developers/page.tsx`, `components/developers/endpoint-doc.tsx`, `components/developers/language-tabs.tsx`, `components/developers/rate-limits-table.tsx`

- [ ] **Step 1: Create EndpointDoc**

Create `components/developers/endpoint-doc.tsx`:
- Props: `endpoint: ApiEndpoint`
- Renders: method badge (colored: GET=green, POST=blue) + path in monospace
- Description text
- If `requestBody`: "Request" heading + `<CodeBlock code={requestBody} language="json" />`
- "Response" heading + `<CodeBlock code={responseBody} language="json" />`
- Styled with left border (color matching method)

- [ ] **Step 2: Create LanguageTabs**

Create `components/developers/language-tabs.tsx` — `"use client"`:
- Props: `examples: Record<string, string>` (key = language name, value = code)
- Tab buttons for each language (JavaScript, Python, Swift)
- Active tab shows corresponding `<CodeBlock>`
- Uses `useState` for active tab

- [ ] **Step 3: Create RateLimitsTable**

Create `components/developers/rate-limits-table.tsx`:
- Simple table from `RATE_LIMITS` data
- Columns: Tier, Requests/min, Requests/day, Burst limit
- Styled consistently with ComparisonTable (dark theme, borders)

- [ ] **Step 4: Create the page**

Create `app/developers/page.tsx`:
- SEO metadata
- Page heading: "Developer Documentation" with subheading
- `id="quickstart"` section: Quick Start guide — 3 steps: sign up, get API key, make first request. Include a `<CodeBlock>` showing a curl command and a JS example.
- Authentication section: JWT explanation with code example showing header
- API Reference section: map `API_ENDPOINTS` to `<EndpointDoc>` components
- Code Examples section: `<LanguageTabs>` with `CODE_EXAMPLES.compatibility`
- Rate Limits section: `<RateLimitsTable />`
- Pricing Calculator section: `<PricingCalculator />`
- `id="sdks"` section: SDK listing — JS/TS (available), Python (available), Swift (coming soon), Kotlin (coming soon). Each with install command in `<CodeBlock>`.
- Style: left sidebar navigation (desktop) for jumping between sections, hidden on mobile

- [ ] **Step 5: Commit**

```bash
cd /c/Users/steph/elodtx-website
git add app/developers/ components/developers/
git commit -m "feat: add Developers page with API docs, code examples, and pricing calculator"
```

---

## Task 16: About Page

**Files:**
- Create: `app/about/page.tsx`, `components/about/timeline.tsx`, `components/about/team-structure.tsx`

- [ ] **Step 1: Create Timeline component**

Create `components/about/timeline.tsx` — `"use client"`:
- Vertical timeline from `ABOUT_TIMELINE`
- Alternating left/right layout on desktop, all-left on mobile
- Each event: year in gold large text, title as h3, description paragraph
- Connected by vertical line with gold dots at each event
- Events stagger in on scroll

- [ ] **Step 2: Create TeamStructure component**

Create `components/about/team-structure.tsx`:
- Two cards side by side (desktop), stacked (mobile):
  - "Urban Space Web Technologies Ltd" — Mauritius flag emoji — "Product, AI, engineering"
  - "Eloclouds Limited" — UK flag emoji — "Infrastructure, partnerships, European operations"
- Connected by a "Partnership" label between them
- Below: investment card — "$7.5M" in large gold text — "Backed by Alice Rose LLC, USA"

- [ ] **Step 3: Create the page**

Create `app/about/page.tsx`:
- SEO metadata
- Page heading: "Built in Mauritius. Designed for the World."
- Origin story section: narrative paragraphs about Baeyond → Âme Intense → EloDtx extraction
- Philosophy section: "Why Connection Intelligence Is Different" — paragraphs explaining purpose-built vs adapted AI
- `<Timeline />`
- `<TeamStructure />`
- Technology section: TurboQuant compression explanation, Qwen 3.5 foundation, multi-agent orchestration — described in a 3-card grid
- Baeyond POC results: reuse proof metrics in a compact display
- Global vision section: closing narrative about starting in Mauritius, chosen over Silicon Valley

- [ ] **Step 4: Commit**

```bash
cd /c/Users/steph/elodtx-website
git add app/about/ components/about/
git commit -m "feat: add About page with timeline, team structure, and origin story"
```

---

## Task 17: Waitlist Page & API Route

**Files:**
- Create: `app/waitlist/page.tsx`, `app/api/waitlist/route.ts`

- [ ] **Step 1: Create API route**

Create `app/api/waitlist/route.ts`:

```typescript
import { NextRequest, NextResponse } from "next/server";

export async function POST(request: NextRequest) {
  try {
    const body = await request.json();

    const { name, company, email, platformType, mauRange, message } = body;

    // Validate required fields
    if (!name || !company || !email || !platformType || !mauRange) {
      return NextResponse.json(
        { error: "Missing required fields" },
        { status: 400 }
      );
    }

    // Validate email format
    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (!emailRegex.test(email)) {
      return NextResponse.json(
        { error: "Invalid email format" },
        { status: 400 }
      );
    }

    // In production, forward to email service, database, or CRM here
    console.log("Waitlist submission:", { name, company, email, platformType, mauRange, message });

    return NextResponse.json({ success: true, message: "Application received" });
  } catch {
    return NextResponse.json(
      { error: "Invalid request" },
      { status: 400 }
    );
  }
}
```

- [ ] **Step 2: Create Waitlist page**

Create `app/waitlist/page.tsx`:
- SEO metadata
- Clean centered layout, max-w-2xl
- Heading: "Be among the first platforms powered by EloDtx."
- Subheading: "First 10 partners receive 90 days free access with full dedicated support."
- `<WaitlistForm />` (full mode, not compact)
- Below form: note in `text-text-muted text-sm`: "Investors attending the Âme Intense launch event in Mauritius, October 2026 will have exclusive first access."

- [ ] **Step 3: Commit**

```bash
cd /c/Users/steph/elodtx-website
git add app/waitlist/ app/api/waitlist/
git commit -m "feat: add Waitlist page and API route"
```

---

## Task 18: SEO, Metadata & Final Polish

**Files:**
- Modify: `app/layout.tsx`
- Create: `app/robots.ts`, `app/sitemap.ts`

- [ ] **Step 1: Add JSON-LD structured data to layout**

In `app/layout.tsx`, add a `<script type="application/ld+json">` in the `<head>` (via metadata or inline) with Organization schema:

```json
{
  "@context": "https://schema.org",
  "@type": "Organization",
  "name": "EloDtx",
  "url": "https://elodtx.com",
  "description": "The AI Connection Intelligence Platform",
  "foundingDate": "2025",
  "foundingLocation": "Mauritius"
}
```

- [ ] **Step 2: Create robots.ts**

Create `app/robots.ts`:

```typescript
import type { MetadataRoute } from "next";

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: "*",
      allow: "/",
    },
    sitemap: "https://elodtx.com/sitemap.xml",
  };
}
```

- [ ] **Step 3: Create sitemap.ts**

Create `app/sitemap.ts`:

```typescript
import type { MetadataRoute } from "next";

export default function sitemap(): MetadataRoute.Sitemap {
  const baseUrl = "https://elodtx.com";
  const pages = [
    "",
    "/how-it-works",
    "/industries",
    "/the-whisper",
    "/proximity",
    "/developers",
    "/about",
    "/waitlist",
  ];

  return pages.map((page) => ({
    url: `${baseUrl}${page}`,
    lastModified: new Date(),
    changeFrequency: "weekly" as const,
    priority: page === "" ? 1 : 0.8,
  }));
}
```

- [ ] **Step 4: Verify full build succeeds**

```bash
cd /c/Users/steph/elodtx-website
npm run build
```

Expected: Build succeeds with no errors. All 8 pages generate.

- [ ] **Step 5: Commit**

```bash
cd /c/Users/steph/elodtx-website
git add app/robots.ts app/sitemap.ts app/layout.tsx
git commit -m "feat: add SEO metadata, robots.txt, sitemap, and JSON-LD structured data"
```

---

## Task 19: Final Build Verification & Polish

- [ ] **Step 1: Run production build**

```bash
cd /c/Users/steph/elodtx-website
npm run build
```

Fix any TypeScript errors, missing imports, or build failures.

- [ ] **Step 2: Test all pages in dev mode**

```bash
cd /c/Users/steph/elodtx-website
npm run dev
```

Navigate to each page and verify:
- `/` — all 9 sections render, animations work, counters count up
- `/how-it-works` — 4 sections render, diagrams animate
- `/industries` — all 6 verticals render, anchor links work
- `/the-whisper` — conversation demo plays, sources diagram animates
- `/proximity` — mockup animates, FAQ accordion works
- `/developers` — code blocks render with syntax highlighting, calculator works
- `/about` — timeline animates, team cards render
- `/waitlist` — form validates, submits to API, shows success state

- [ ] **Step 3: Fix any issues found**

Address any rendering issues, missing styles, broken animations, or layout problems.

- [ ] **Step 4: Final commit**

```bash
cd /c/Users/steph/elodtx-website
git add -A
git commit -m "fix: final polish and build verification"
```
